# Crash analysis — Issue #13262 "Game crash" (VP 5.4.2)

**Verdict:** Real crash, NOT a discrete DLL bug — 32-bit virtual address space
exhaustion (known-issue class). The `eip=0` execute-AV is a call through a
NULL function pointer after an allocation failure; effectively a duplicate of
the systemic OOM class also seen in #13270 and #13344.

## Dump

`CvMiniDump_20260801_171010_5.4.2-0-ga8a136d02_Release.dmp`, analyzed with the
matching official 5.4.2 Release PDB (DLL base 0x6EE20000, size 0x1388000,
RSDS GUID `32A51E99E75AAEAB4C4C44205044422E` — exact PDB match).

```
Exception 0xC0000005 (Access Violation), execute (DEP) of address 0x0
Thread 0x5f08
eax=0x00000000  ebx=0x0214E408  ecx=0x00000000  edx=0x02761090
esi=0x02761080  edi=0x02589000  ebp=0x00000000  esp=0x071FFCCC
eip=0x00000000  eflags=0x00010202
```

`eip = 0` with an execute-AV means control jumped to address 0 — a call
through a NULL function pointer / corrupted vtable slot. There is no faulting
instruction to symbolize.

## The `???+0xfffff400` crashes.log entries decoded

The recurring `???+0xfffff400` lines in this user's crashes.log are this same
signature: the in-game logger subtracts the .text VA-to-file delta (0xC00)
blindly, and `0 - 0xC00` wraps to `0xfffff400`. So those entries are all
`eip=0` crashes. (Logger-side improvement: special-case eip=0; the file-offset
handling was addressed in PR #13347's crash-handler changes.)

## Call-site-verified stack frames

x86 has no reliable unwind for this state, so these are return-address
candidates validated as "preceded by a `call` instruction into the DLL",
symbolized with the official 5.4.2 PDB:

```
0x721a0c4  CvGameCore_Expansion2.dll+0x8a7961  CvPlayer::setTurnActive+0x5891
0x725c600  CvGameCore_Expansion2.dll+0x8c6e61  CvPlayer::doTurnPostDiplomacy+0x102f1
0x7218f70  CvGameCore_Expansion2.dll+0x8e736e  CvPlayer::doInstantYield+0xa28e
0x7219840  CvGameCore_Expansion2.dll+0x8e6552  CvPlayer::doInstantYield+0x9472
0x7251778  CvGameCore_Expansion2.dll+0x8d6373  CvPlayer::DoEvents+0x2b23
0x72616f8  CvGameCore_Expansion2.dll+0x8d526d  CvPlayer::DoEvents+0x1a1d
```

i.e. AI turn processing (`setTurnActive → doTurn → doTurnPostDiplomacy →
DoEvents / doInstantYield`) on a very late-game save (AD 1794). The same
verified frames reproduce independently in threads 0x2c1c and 0x4e8c.
("CvSyncVar<...>::compare/debugDump" names appearing in a raw pointer scan are
nearest-public artifacts, not real frames.)

## Memory markers at the crash (crashes.log, same timestamp)

```
Sub2G: committed 1,682,680k / free 34,736k / largest free block 10,432k
Total: committed 2,044,172k of 4,194,176k
```

## History across the 29 crashes.log entries

- 15+ crashes with the identical `eip=0` signature spanning 5.3.3 → 5.4.2
- plus `lua51_Win32.dll+0x2f05` entries (= `luaL_unref` writing through a
  stale `lua_State::top`; see the #13338 secondary-signature analysis)
- plus several `CvGameDatabase` offsets

A "signature roulette" across modules for one user, with the largest free
block repeatedly collapsing to 428–768 KB. Tens of MB "free" but a sub-1MB
largest contiguous block = severe VA fragmentation: an allocation fails, a
stale/never-constructed object's function pointer is 0, and the crash lands
wherever the next indirect call goes.

This also explains the reporter's observation that a PC restart sometimes
helps: a fresh process gets an unfragmented 4 GB (LAA) address space, which
then degrades again while playing/loading.

## Conclusion

Real crash, but the root cause is memory exhaustion/fragmentation driven by
late-game live-object quantity — no specific code defect at the crash site.
The mitigation track is memory-reduction/diagnostics work (e.g. #13347's
tactical-plot reserve, MemoryPressure/MemoryAttribution instrumentation), not
a point fix.
